import { TelValidatorDirective } from './tel-validator.directive';

describe('TelValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TelValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
